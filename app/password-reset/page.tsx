import ResetPasswordViewPage from './_components/reset-password-view';

export const metadata = {
  title: 'Redefinir senha'
};

export default async function Page() {
  return <ResetPasswordViewPage />;
}
